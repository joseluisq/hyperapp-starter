{
  "name": "hyperapp-starter",
  "version": "0.0.0",
  "private": true,
  "description": "Minimalistic Hyperapp, Typescript and Parcel starter.",
  "author": "Jose Quintana <git.io/joseluisq>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist .cache",
    "prestart": "yarn clean",
    "start": "parcel public/index.html",
    "prebuild": "yarn clean",
    "build": "env NODE_ENV=production parcel build public/index.html --no-source-maps --experimental-scope-hoisting --public-url ./",
    "lint": "tslint --format stylish --project tsconfig.json",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "hyperapp": "^1.2.6"
  },
  "devDependencies": {
    "@types/jest": "^23.1.3",
    "@types/node": "^10.5.1",
    "autoprefixer": "^8.6.4",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^23.2.0",
    "parcel-bundler": "^1.9.3",
    "parcel-plugin-typescript": "^1.0.0",
    "ts-jest": "^22.4.6",
    "tslint": "^5.10.0",
    "tslint-config-standard-plus": "^1.2.0",
    "typescript": "^2.9.2"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testMatch": [
      "**/test/**.spec.+(ts|tsx|js)"
    ],
    "moduleNameMapper": {
      "\\.(css|less|scss|sass)$": "identity-obj-proxy"
    },
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "json",
      "node"
    ]
  }
}
